#include <iostream>
#include <algorithm>
#include <vector>
#include <cstring>
#include <unordered_map>

using namespace std;
vector<int> a;

vector<int> getTime(string s, int maxT) {
    int n = s.size();
    vector<int> t;
    if (s[0] == '*') {

    } else {
        t.push_back(getNumber(s));
                
    }
}

int main() {
    
    return 0;
}